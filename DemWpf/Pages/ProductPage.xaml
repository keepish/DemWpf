<Page x:Class="DemWpf.Pages.ProductPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DemWpf.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:m="clr-namespace:DemWpf.Models"
      Title="Товары">

    <Page.Resources>
        <Style x:Key="PriceTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasDiscount}" Value="False">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>
    
    <Grid Margin="0,24,0,-24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10" HorizontalAlignment="Right">
            <TextBlock x:Name="UserNameText" VerticalAlignment="Center"/>
            <Image Source="/Icon.png" Stretch="Uniform" Height="40"/>
        </StackPanel>

        <StackPanel x:Name="FilterStackPanel" Orientation="Horizontal" Margin="10" Grid.Row="1">
            <TextBlock Text="Поиск "/>
            <TextBox x:Name="SearchTextBox" Width="300" Margin="5,0" TextChanged="FilterChanged"/>
            <TextBlock Text="Поставщик"/>
            <ComboBox x:Name="SupplierFilterBox" Width="150" SelectionChanged="FilterChanged"/>
            <TextBlock Text="Количество"/>
            <ComboBox x:Name="SortBox" Width="150" SelectionChanged="FilterChanged">
                <ComboBoxItem Content="Без сортировки"/>
                <ComboBoxItem Content="По возрастанию"/>
                <ComboBoxItem Content="По убыванию"/>
            </ComboBox>
        </StackPanel>

        <ListView x:Name="ProductsListView" Grid.Row="2" HorizontalContentAlignment="Stretch" ScrollViewer.CanContentScroll="False" MouseDoubleClick="ProductsListView_MouseDoubleClick">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="#7FFF00"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBigDiscount}" Value="True">
                            <Setter Property="Background" Value="#2E8B57"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Amount}" Value="0">
                            <Setter Property="Background" Value="LightBlue"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            
            <ListView.ItemTemplate>
                <DataTemplate DataType="m:Product">
                    <Border BorderBrush="Black" BorderThickness="2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="120"/>
                            </Grid.ColumnDefinitions>

                            <Border BorderBrush="Black" BorderThickness="2" Margin="10">
                                <Image Source="{Binding PhotoPath}" Stretch="Uniform"/>
                            </Border>

                            <Border Grid.Column="1" Margin="0,10" BorderBrush="Black" BorderThickness="1">
                                <StackPanel Margin="10">
                                    <TextBlock FontWeight="Bold" FontSize="14">
                                        <Run Text="{Binding Category.Name}"/>
                                        <Run Text=" | "/>
                                        <Run Text="{Binding Name}"/>
                                    </TextBlock>
                                    <TextBlock Text="Описание товара:"/>
                                    <TextBlock Text="{Binding Description}" 
                                               TextWrapping="Wrap"/>
                                    <TextBlock>
                                        <Run Text="Производитель:"/>
                                        <Run Text="{Binding Manufacturer.Manufacturer1}"/>
                                    </TextBlock>
                                    <TextBlock>
                                         <Run Text="Поставщик:"/>
                                         <Run Text="{Binding Supplier.Supplier1}"/>
                                    </TextBlock>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Цена: "/>
                                        <TextBlock Text="{Binding Price, StringFormat=C}" Foreground="Red"
                                                   TextDecorations="Strikethrough" Style="{StaticResource PriceTextStyle}"/>
                                        <TextBlock Text=" "/>
                                        <TextBlock Text="{Binding DiscountedPrice, StringFormat=C, Mode=OneWay}"/>
                                    </StackPanel>
                                    <TextBlock>
                                         <Run Text="Единица измерения:"/>
                                         <Run Text="{Binding Unit}"/>
                                    </TextBlock>
                                    <TextBlock>
                                         <Run Text="Количество на складе:"/>
                                         <Run Text="{Binding Amount}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2" BorderBrush="Black" BorderThickness="2" Margin="10">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock FontSize="20">
                                        <Run Text="{Binding Discount}"/>
                                        <Run Text="%"/>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
