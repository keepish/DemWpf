<Page x:Class="DemWpf.Pages.EditOrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DemWpf.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EditOrderPage">

    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Артикул"/>
            <ComboBox x:Name="ArticleBox"
                      DisplayMemberPath="Article"
                      SelectedValuePath="Productid"/>

            <TextBlock Text="Статус заказа"/>
            <ComboBox x:Name="StatusBox"/>

            <TextBlock Text="Пункт выдачи"/>
            <ComboBox x:Name="PickUpPointBox" DisplayMemberPath="Address"/>

            <TextBlock Text="Дата заказа"/>
            <DatePicker x:Name="OrderDatePicker"/>

            <TextBlock Text="Дата выдачи"/>
            <DatePicker x:Name="DeliveryDatePicker"/>

            <StackPanel Orientation="Horizontal" Margin="10">
                <Button Content="Сохранить" Click="Save_Click" Width="100" Margin="5"/>
                <Button x:Name="DeleteButton" Content="Удалить" Click="Delete_Click" Width="100" Margin="5"/>
                <Button Content="Назад" Click="Back_Click" Width="100" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
